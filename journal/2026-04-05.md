# 2026-04-05

## Session 1: Project Setup and Phase 0 Planning

**Model**: Claude Opus 4.6 (1M context)

### What happened

- Reviewed the master plan in `.github/prompts/plan-tuvXCppSolverRewrite.prompt.md` and all 9 phase prompts
- Established branching strategy: `cpp-rewrite` as integration branch, `phase0-scaffolding` (and future `phaseN-*`) as feature branches
- Created both branches, pushed `phase0-scaffolding` to NCAR remote

### Key decisions

- **Fortran preserved**: `src/` will be renamed to `fortran/` — Fortran stays in the tree as reference through all phases, removed only after Phase 8
- **Clean C++ layout**: `src/` reclaimed for C++ `.cpp` files, `include/tuvx/` for headers — no mixing with Fortran at any level
- **Validation via Fortran reference data**: build and run Fortran, capture outputs as CSV in `test/reference/phaseN/`, committed to git. Tolerances empirical, no bit-for-bit requirement.
- **Phase 0 has no numerical validation** — it's scaffolding. Reference data generation starts at Phase 1.
- **Phase lifecycle**: plan → test plan → generate reference data → implement → PR → human review → merge
- **Cross-model review**: Claude codes, Codex (and others) review plans and code
- **Plans are session-resumable**: enough context to initialize a fresh session; updated mid-session if interrupted

### Artifacts

- `plan/phase0.md` — revised Phase 0 implementation plan
- `CLAUDE.md` — project conventions, workflow, validation strategy
- `HISTORY.md` — this file's index
- GitHub issue NCAR/tuv-x#174 — Phase 0 tracking

### Zen

*"Before enlightenment, chop wood, carry water. After enlightenment, chop wood, carry water."*

### What's next

- Codex review of Phase 0 plan and project conventions
- Revisit after review — finalize Phase 0 or begin Phase 1 planning
- Phase 0 implementation when the user gives the go-ahead

---

## Session 2: Phase 0 Implementation

**Model**: Claude Opus 4.6 (1M context)

### What happened

- Discussed the Codex Review I findings — all five points were valid and already reflected in the plan
- Decided to keep CamelCase for function naming (matching existing API)
- Implemented the full Phase 0 scaffold in three commits:
  1. **Core scaffold** (`8beb6a1`): `src/` → `fortran/` rename, C++20-only CMakeLists.txt, stripped yaml-cpp/netcdf-fortran/OpenMP/MPI, header-only `musica::tuvx` library, `Array1D<T>` with 7 tests, `RadiatorState` → `ConstituentState` rename, deleted dead stubs
  2. **CI and config** (`e37050d`): Ubuntu/macOS/Windows/Docker/clang-tidy/clang-format workflows, expanded `.clang-tidy` (full check suite, CamelCase), `codecov.yml` (95% target), stripped Dockerfiles of Fortran/yaml-cpp/MPI, Doxygen comments on array headers, README rewrite
  3. **Codex Review II fixes** (`32aec99`): removed `|| true` from clang-tidy CI, removed `data/` from packaging install, documented CUDA/HIP as intentional placeholders, updated plan to match `Size()` over `NumElements()`

### What broke

- GTest linked as shared library against a stale system install (1.11.0 dylib via `FIND_PACKAGE_ARGS`). Fixed by removing `FIND_PACKAGE_ARGS` to force FetchContent build from source as static lib.
- Working directory shifted during `ctest` calls causing "not a directory" errors. Fixed by using absolute paths.

### Key decisions

- **Radiator → Constituent**: adopted throughout C++ code, documented in CLAUDE.md as incremental rename policy
- **Bulk operations API deferred**: ForEachRow, ColumnView, operator[] proxies pushed to Phase 1 when actual solver needs will drive the API surface
- **HostArrayPolicy deferred**: concrete types (Array2D, Array3D) used directly until CUDA/HIP is needed
- **AGENTS.md**: established shared handoff protocol with review thread naming convention (Roman numerals)
- **`cpp-rewrite` pushed to remote**, PR created for `phase0-scaffolding` → `cpp-rewrite`

### Artifacts

- 3 commits on `phase0-scaffolding` (scaffold, CI/config, review fixes)
- `AGENTS.md` — multi-agent collaboration protocol
- `codecov.yml` — coverage configuration
- `include/tuvx/util/array1d.hpp` + `test/unit/util/array1d.cpp` — new Array1D type
- `include/tuvx/radiative_transfer/constituent.hpp` — renamed from radiator.hpp
- PR pending: `phase0-scaffolding` → `cpp-rewrite`

### Validation

- 6/6 C++ tests pass: `cxx_grid`, `cxx_profile`, `error_function`, `util_array1d`, `util_array2d`, `util_array3d`
- CI not yet validated on GitHub Actions (PR not yet created via web)

### Zen

*"In the beginner's mind there are many possibilities, but in the expert's mind there are few."*

### What's next

- ~~Create PR via GitHub web UI (gh cli lacks collaborator permissions)~~ Done — NCAR/tuv-x#175
- Wait for CI to pass
- Human review of the PR
- Merge into `cpp-rewrite`, begin Phase 1 planning

---

## Session 2 (continued): CI fixes and PR

- Pushed `cpp-rewrite` to remote, created PR NCAR/tuv-x#175 via `gh` CLI (authenticated as `dwfncar`)
- Fixed three CI failures:
  - **clang-tidy**: added `[[nodiscard]]`, default member init, `std::move`, missing includes; suppressed checks incompatible with design (public members, include-cleaner, identifier-length); exempted `begin`/`end` from CamelCase via `FunctionIgnoredRegexp`
  - **ReadTheDocs**: removed `gfortran` from `environment.yml`, pointed Doxygen INPUT_DIR to `include/` instead of empty `src/`
  - **Dockerfile.coverage**: added `--ignore-errors mismatch` for GCC 15/lcov compatibility, fixed `BASE_DIRECTORY` to `include/`
- All other CI checks pass: Ubuntu (GCC 12-14, Clang 17-18), macOS (Apple Clang, GCC 14), Windows (MSVC, MinGW), Docker (base, memcheck, Intel, NVHPC)
