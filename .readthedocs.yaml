# Minimal config to prevent RTD build failures.
# Documentation will be rebuilt from scratch in a later phase.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

# No Sphinx or other doc build — just succeed quietly
